'use strict';

/**
* create an sql query which
* - create the tables people, organisations, tags_organisations if not defined yet
* - add some content to the table if options.tableReset is true and options.people/organisations/tags_organisations is defined and non empty
*/

module.exports = function (options, cb) {

};
